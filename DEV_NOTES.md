# 개발자 노트 — 2차 배타오픈

**2026 제주한라대학교 제41대 다온 제휴 리스트**  
작성일: 2026-08-11

---

## 서문

본 문서는 제41대 다온 총학생회 제휴 리스트 웹사이트의 **2차 배타오픈**에 대해, 이용자·운영자·후속 개발자가 참고할 수 있도록 정리한 개발자 노트입니다. 1차 오픈 이후 장애 복구와 기본 제휴 목록 서비스가 안정화되었고, 2차 배타에서는 **운영·모더레이션(IP·신고)**, **모바일 이용 경험**, **스크롤·탐색 UX**, **화면 크기별 Beta 설정**을 중심으로 기능을 확장했습니다.

운영 도메인은 **https://chu-p.kro.kr** 이며, 2차 배타 기간에는 메인 공지·배너 등에 「2차 오픈베타 테스트 진행 중」과 같은 안내를 게시할 수 있습니다. **기기별 즐겨찾기**는 Supabase SQL 없이 동작합니다. 그 외 Beta 기능은 관리자 설정 및 Supabase SQL 선행 실행이 필요한 경우가 많으므로, 아래 본문과 SQL 목록을 함께 확인해 주세요.

---

## 1. 2차 배타오픈의 방향

2차 배타는 “기능을 많이 추가한다”기보다, **실제 학우들이 쓰는 과정에서 드러난 불편**과 **운영진이 게시판·후기를 관리하는 데 필요한 도구**를 보완하는 데 초점을 맞췄습니다.

이용자 입장에서는 모바일에서 카테고리를 고르고, 지역을 펼쳐 검색하고, 제휴 업체 목록을 넘기며 읽는 흐름이 자연스러워야 합니다. iPhone 13 mini처럼 가로 375px 이하의 좁은 화면에서는 상단 사이트 제목과 설정(⚙️) 버튼이 겹치지 않아야 하고, 페이지를 바꿀 때마다 목록 맨 위가 보이도록 스크롤이 따라와야 합니다.

운영진 입장에서는 악성 게시·도배·부적절한 후기에 **신고**로 접수하고, **IP 단위**로 활동을 추적·차단할 수 있어야 합니다. 신고 목록과 IP 관리는 게시글 관리 화면과 분리된 **전용 관리자 메뉴**에서 처리하도록 구성했습니다.

---

## 2. IP 관리 기능

### 2.1 도입 배경

게시판과 제휴 후기는 익명 또는 닉네임 기반으로 작성됩니다. 동일인이 반복적으로 문제 콘텐츠를 올리는 경우, 게시글만 하나씩 지우는 방식으로는 대응이 늦어질 수 있습니다. 2차 배타에서는 **작성 IP를 기록**하고, 관리자가 **IP별 활동을 조회·차단**할 수 있는 흐름을 추가했습니다.

이용자에게 IP 주소가 화면에 노출되지는 않습니다. 차단된 IP에서 글·댓글·후기 작성을 시도하면, 서버 단에서 거부되고 안내 메시지가 표시됩니다.

### 2.2 기록 대상

IP 활동 로그는 대략 다음 범주를 추적합니다.

- **게시글** 작성
- **댓글** 작성
- **제휴 후기** 작성

관리자 IP 관리 화면에서는 IP별로 위 활동의 횟수, 최근 활동 시각, 게시글 상태(정상·제한·차단 등), 차단 여부를 한눈에 볼 수 있습니다. 특정 IP를 선택하면 해당 IP의 게시글·댓글·후기 목록을 펼쳐 확인할 수 있습니다.

### 2.3 차단·해제

IP 차단 시 **사유**를 입력하도록 되어 있습니다. 차단·해제 이력과 사유는 운영 기록으로 남기며, 차단 IP 목록은 별도 패널에서 일괄 관리할 수 있습니다.

개발자 모드에서 `board_ip_moderation_enabled` 항목으로 IP 모더레이션 기능 전체를 켜거나 끌 수 있습니다. SQL이 적용되지 않은 DB에서는 저장·동작 오류가 날 수 있으므로, 배타 오픈 전 `board-ip-moderation.sql`, `ip-activity-log.sql`, `content-ip-ban-guard.sql` 등 관련 마이그레이션 실행을 권장합니다.

### 2.4 관리자 접근 경로

관리자 → **게시판** → **IP 관리**

게시글 관리와 분리되어 있어, “신고를 확인하다 IP까지 추적·차단한다”는 운영 흐름을 한 메뉴 안에서 이어갈 수 있습니다.

---

## 2.5 기기 관리 기능 (voter key)

IP 관리와 동일한 패턴으로, 브라우저 **기기 키**(`board-voter-key`, localStorage UUID) 단위로 비회원 게시글·댓글·후기 작성을 차단할 수 있습니다.

- **차단 목록** — `banned_voter_keys` 테이블
- **활동 로그** — `voter_key_activity_log` (게시글·댓글·후기)
- **관리자 메뉴** — 게시판 → **기기 관리**
- **게시글/후기 관리** — 각 항목에서 「기기 차단」 버튼 (저장된 키가 있을 때)

IP 모더레이션 설정과 무관하게 차단 목록은 항상 적용됩니다. 개발자 모드 **「비회원 기기 관리」** 토글로 기기 키 저장·관리자 표시를 켜고 끌 수 있습니다. SQL: `supabase/device-voter-key-ban.sql`, `board-device-moderation-setting.sql` (기존 `board-ip-moderation.sql`, `partner-reviews.sql` 적용 후 실행).

---

## 3. 신고 기능

### 3.1 도입 배경

학우들이 문제 게시글·댓글·후기를 발견했을 때, 운영진에게 연락 채널(DM·카톡 등)만으로 접수하면 누락·지연이 생기기 쉽습니다. 2차 배타에서는 **사이트 안에서 바로 신고**하고, 운영진은 **신고 관리** 화면에서 모아 처리하는 구조를 만들었습니다.

### 3.2 이용자 신고 흐름

신고 대상은 다음과 같습니다.

- **게시판 게시글** — 게시글 상세 화면에서 신고
- **댓글** — 댓글 메뉴의 신고
- **제휴 후기** — 후기 신고 (관련 설정·SQL 적용 시)

신고 시 관리자가 미리 설정해 둔 **사유 목록**에서 고르거나, 직접 사유를 입력할 수 있습니다. 신고가 접수되면 설정된 **완료 안내 문구**(`board_report_success_message`)를 이용자에게 보여 줍니다. 동일 항목에 대한 **중복 신고**는 허용하지 않습니다.

### 3.3 관리자 신고 관리

관리자 → **게시판** → **신고 관리**

신고 관리 화면은 2차 배타에서 **독립 패널**로 구성되었습니다. 게시글·댓글·제휴 후기에서 들어온 신고를 한 목록에서 확인하고, 미처리 건수를 파악할 수 있습니다.

운영자는 신고를 **검토 완료**로 표시하고, 필요 시 **조치 사유**(`admin_action_reason`)를 남길 수 있습니다. 관리자가 직접 작성·조치한 항목과 이용자 신고를 구분할 수 있도록 `is_admin_created` 등 관리 필드가 포함되어 있습니다.

신고 **사유 목록**과 **완료 안내 문구**는 신고 관리 화면 또는 site_settings에서 편집·저장할 수 있습니다. SQL 미적용 시 저장 실패 메시지에 필요한 파일명이 안내됩니다 (`site-settings-save-error.ts` 참고).

### 3.4 관련 SQL

`board-report-reasons-setting.sql`, `board-reports-and-comment-ip.sql`, `board-reports-admin-fields.sql`, `partner-review-reports-and-ip.sql`, `content-admin-action-reason.sql` 등을 순서에 맞게 적용해 주세요.

---

## 4. 모바일 기능 개선

### 4.1 전체 레이아웃

모바일에서 상단은 **메뉴(제목 pill·드롭다운) → 배너 → 공지(Notice) → 카테고리 → 제휴 목록** 순으로 읽히도록 정리했습니다. 1차 대비 큰 변화는 **히어로 배너를 상단 nav 아래 별도 블록**으로 분리한 것입니다. nav와 배너를 한 덩어리로 겹치거나 잘리던 문제를 줄이고, 공지 캐러셀·카테고리 버튼과의 간격도 모바일 구간 CSS에서 조정했습니다.

PC(1280px 이상)에서는 nav와 배너가 가로로 넓게 펼쳐지고, 배너 이미지는 `object-fit: cover`로 잘리지 않게 표시됩니다.

2차 배타 기준 **상단 메뉴 sticky(스크롤 고정)는 비활성**입니다. 스크롤할 때 메뉴가 붙었다 떨어지며 레이아웃이 흔들리던(jitter) 현상을 피하기 위한 선택입니다. DB·관리자에 sticky 관련 설정은 남아 있으나, 현재 프론트엔드는 `stickyEnabled={false}`로 고정해 두었습니다.

### 4.2 제휴 목록·검색·필터

모바일 제휴 목록 상단에는 **카테고리 버튼**(전체, 음식점, 카페/디저트 등) 그리드가 있습니다. **지역별 상세 검색**은 Beta 기능으로, 「펼치기」로 지역·세부 지역을 선택할 수 있습니다.

제휴 **검색**은 상단 nav가 아니라 **목록 툴바**에 있습니다. 정렬(최신순·오래된순·추천순·새로고침) 줄 **왼쪽 돋보기**를 누르면 검색창이 펼쳐집니다. PC에서도 동일한 패턴을 사용하며, 상단 nav 검색은 제거했습니다.

검색창 UX 관련 2차 배타 변경: 바깥 영역을 탭해도 검색창이 **자동으로 닫히지 않습니다**. 돋보기를 다시 누르거나, 입력 내용이 없을 때 Esc 키로 닫습니다. 모바일에서는 placeholder 예시 문구를 비워 두어, 좁은 화면에서 불필요한 텍스트가 겹치지 않게 했습니다.

### 4.3 iPhone mini (375px 이하)

iPhone 12 mini, 13 mini 등 **가로 375px 이하** 기기에서 「2026 제주한라대학교 제41대 다온 제휴 리스트」 제목 pill과 우측 상단 **설정(⚙️) 버튼** 테두리가 겹쳐 보인다는 피드백이 있었습니다.

이에 `@media (max-width: 375px)` 구간에서 nav 오른쪽 여백을 늘리고, 제목 글자 크기·설정 버튼 크기를 소폭 줄이는 **CSS 자동 보정**을 적용했습니다. 이 겹침 수정은 **관리자에서 켜거나 끌 수 있는 설정이 없으며**, 해당 해상도에서 항상 적용됩니다.

제휴 **목록**의 페이지당 개수·열 수·혜택 칸 높이만, 관리자 「아이폰 미니 제휴 목록 설정」(Beta)에서 별도로 조정할 수 있습니다. 겹침 CSS와 목록 Beta 설정은 서로 다른 레이어입니다.

### 4.4 화면 크기별 제휴 목록 Beta (관리자)

관리자 → 제휴 → **목록·지도·정렬** 에서 구간별 Beta 설정을 제공합니다.

- **모바일** (376~767px)
- **아이폰 미니** (375px 이하)
- **폴드·태블릿** (768~1279px)
- **와이드·4K** (1920px 이상)

각 구간마다 「별도 설정 사용」을 켜야 해당 구간 전용 값(페이지당 개수, 한 줄 카드 개수, 혜택 칸 최소 높이 등)이 적용됩니다. 끄면 PC 기본값 또는 모바일 공통 기본값으로 동작합니다. SQL 파일(`partner-mobile-settings.sql`, `partner-mini-settings.sql`, `partner-tablet-settings.sql`, `partner-wide-settings.sql`) 적용 후 저장할 수 있습니다.

---

## 5. 스크롤·페이지 이동 개선

### 5.1 페이지 이동 시 목록 상단으로 스크롤

제휴 목록이 여러 페이지(예: 전체 55건 · 1/7 페이지)로 나뉠 때, 「다음」을 눌러도 화면이 아래쪽에 머물면 학우들이 새 페이지 첫 카드를 놓치기 쉽습니다.

2차 배타에서는 **페이지네이션으로 페이지를 바꿀 때**, 제휴 목록 또는 게시판 구역의 **앵커 위치로 부드럽게 스크롤**되도록 했습니다. 구현은 `Pagination`의 `scrollTargetId`와 `scroll-to-section.ts`를 사용하며, 제휴 목록은 `#partner-list`, 게시판은 `#board-section-anchor` 등을 대상으로 합니다.

관리자 → 제휴 → 목록·지도·정렬 의 **「페이지 이동 시 상단으로 스크롤」** (`pagination_scroll_top_enabled`)으로 이 동작을 끌 수 있습니다. 기본값은 **켜짐**입니다.

### 5.2 상단 이동 버튼

`FloatingPageControls`는 일정 이상 아래로 스크롤했을 **「맨 위로」** 버튼을 표시합니다. 긴 목록·게시판을 읽다가 다시 상단 배너·메뉴 쪽으로 돌아갈 때 사용합니다.

### 5.3 sticky nav 비활성과의 관계

상단 메뉴를 스크롤에 고정(sticky/fixed)하지 않음으로써, 모바일에서 주소창·툴바 높이 변화와 겹치며 생기던 **스크롤 튐**을 줄였습니다. 대신 페이지 이동 시 **목록 앵커로 스크롤**하는 방식으로 “다시 위로 올라가기” 부담을 줄이는 쪽을 택했습니다.

---

## 6. 설정 패널(⚙️)과 실험실 Beta

우측 상단 설정 버튼은 관리자에서 `settings_panel_enabled`가 켜져 있을 때만 표시됩니다. 그 안 **실험실** 섹션에는 다크 모드, 사이트 크기 조절, 모바일 PC 모드, 배경·상단 메뉴 배경 등 Beta 항목이 있으며, 항목마다 관리자에서 개별 ON/OFF가 가능합니다.

2차 배타에서 다크 모드일 때 설정 패널 글자·Copyright·라디오 버튼 등 **가독성**을 보강했고, 사이트 전체 크기를 키워도 설정 패널 자체 글자 크기는 16px로 고정해 UI가 깨지지 않게 했습니다.

---

## 7. 제휴 상세·4K·기타

**제휴 상세 추가 사진**은 `partner_photos` 테이블과 갤러리 UI(`PartnerPhotoGallery`)로 제공됩니다. 목록 카드는 대표 이미지(`image_url`)만 쓰고, 상세에서 1/N 갤러리로 추가 사진을 넘겨 볼 수 있습니다. 관리자 업체 관리에서 「상세 추가 사진」 다중 업로드가 가능합니다. `partner-photos.sql` 선행 필요.

**4K·울트라와이드**에서는 메인·nav 최대 너비를 단계적으로 키우고, 제휴 그리드는 넓은 화면에서 최대 6열까지 자동 배치됩니다. 와이드 구간 전용 페이지당 개수는 관리자 Beta 설정으로 조정할 수 있습니다.

### 7.1 기기별 즐겨찾기 (localStorage)

회원 로그인 없이 **「내가 찜한 제휴 업체」** 를 이 기기·이 브라우저에만 저장하는 기능입니다. Supabase 테이블·SQL **추가 없음** — 서버 API 호출도 ♥ 누를 때 발생하지 않습니다.

**이용 방법**

- 제휴 카드 **사진 우측 상단 ♥** — 누르면 즐겨찾기 추가/해제 (채워지면 분홍색)
- 목록 툴바 **「♥ 즐겨찾기 N」** — 찜한 업체만 필터 (다시 누르면 전체 목록)

**저장 방식 (기기별)**

| 항목 | 내용 |
|------|------|
| 저장 위치 | 브라우저 `localStorage` |
| 키 | `jeju-halla-partner-favorites` |
| 값 | 제휴 업체 ID 배열 JSON (해시 아님) |
| 코드 | `src/lib/device-storage.ts`, `src/lib/partner-favorites.ts` |

PC·폰·브라우저마다 **별도 목록**입니다. 브라우저 데이터 삭제 시 찜 목록도 사라집니다. IP와 무관하며, IP 관리·신고 기능과 데이터가 섞이지 않습니다.

**IP별 기능과의 구분:** 글·댓글·후기·신고는 서버 IP 기록, 즐겨찾기는 기기 localStorage만 사용합니다.

---

## 8. 공지용 안내 문 (복사용)

아래 문구는 관리자 메인 공지·설정 패널 하단 등에 그대로 붙여 넣을 수 있습니다.

---

**[2차 배타오픈 안내]**

안녕하세요. 제41대 다온 총학생회 제휴 리스트입니다.

2차 배타오픈을 진행하며 서비스를 보강했습니다.

**이용 편의:** 제휴 목록 검색·지역별 필터, **기기별 즐겨찾기(♥)**, 모바일·PC 화면 배치 개선, 페이지 이동 시 목록 상단으로 자동 스크롤, 제휴 업체 추가 사진 갤러리, iPhone mini 등 좁은 화면 레이아웃 수정.

**운영·안전(Beta):** 게시글·댓글·제휴 후기 신고, 악성 이용 대응을 위한 IP 관리(관리자 전용). Beta 기능은 테스트 중이며, IP 주소는 이용자에게 노출되지 않습니다.

불편한 점은 게시판 문의 또는 Instagram DM으로 알려 주세요. 감사합니다.

---

## 9. Supabase SQL — 배타 오픈 전 체크리스트

2차 배타 기능 대부분은 `site_settings` 및 관련 테이블 **컬럼·테이블 추가**에 의존합니다. 아래를 Supabase SQL Editor에서 필요 순으로 실행해 주세요.

**IP·신고 (우선)**  
`board-ip-moderation.sql` → `ip-activity-log.sql` → `content-ip-ban-guard.sql` → `device-voter-key-ban.sql` → `board-device-moderation-setting.sql` → `board-reports-and-comment-ip.sql` → `board-report-reasons-setting.sql` → `board-reports-admin-fields.sql` → `partner-review-reports-and-ip.sql` → `content-admin-action-reason.sql`

**제휴 목록 구간 Beta**  
`partner-mobile-settings.sql` → `partner-mini-settings.sql` → `partner-tablet-settings.sql` → `partner-wide-settings.sql`

**제휴 상세 사진**  
`partner-photos.sql`

**기기별 즐겨찾기**  
SQL **불필요** (브라우저 localStorage만 사용)

**일괄(미적용 DB)**  
`pending-developer-mode-columns.sql`

관리자 저장 시 「컬럼이 없습니다」 오류가 나면 `src/lib/site-settings-save-error.ts`에 안내된 SQL 파일명을 실행하면 됩니다.

---

## 10. 알려진 제한·유의사항

IP 모더레이션·신고·구간별 제휴 설정은 SQL 없이는 저장·동작하지 않을 수 있습니다. **기기별 즐겨찾기**는 SQL 없이 동작하며, 찜 목록은 Supabase에 저장되지 않습니다. iPhone mini **제목·설정 버튼 겹침**은 CSS로만 처리되며 관리자 토글이 없습니다. **상단 sticky**는 DB 설정은 있으나 현재 프론트에서는 꺼져 있습니다. **지역별 상세 검색**·구간별 목록 설정은 UI에 Beta 표시가 붙어 있으며, 운영 정책에 맞게 공지와 함께 안내하는 것을 권장합니다.

파비콘(탭 아이콘)과 브랜드 아이콘(제목 pill)은 역할을 혼용하지 않습니다. 제휴 검색은 상단 nav가 아닌 **목록 툴바 돋보기**만 사용합니다.

---

## 11. 후속 검토 (2차 배타 이후)

상단 sticky dead code 정리 또는 `site_nav_sticky_enabled` 재연결 여부, Supabase Auth Site URL과 운영 도메인(`chu-p.kro.kr`) 완전 통일, Beta 구간 설정의 기본값·공식 오픈 전 정책 확정 등은 2차 배타 피드백 수집 후 검토할 수 있습니다.

---

*2차 배타오픈 개발자 노트 · 2026-08-11*
